<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vendor.greasemonkey.drabstech.com.greasemonkeyvendor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".helpscreen.WelcomeActivity"
            android:label="@string/title_activity_welcome" />
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".registration.RegistrationActivity" />
        <activity android:name=".mobileVerification.OTPVerificationActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".vendor_detail.RegisterAddressActivity" />
        <activity android:name=".vendor_detail.ServiceDetailActivity" />
        <activity android:name=".vendor_detail.BikeListActivity" />
        <activity android:name=".servicing_request.RequestDetailActivity"></activity>
    </application>

</manifest>